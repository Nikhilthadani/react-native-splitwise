import { StyleSheet, Text, View } from "react-native";
import React, { useState } from "react";
import { Button } from "react-native-paper";

const FabDebug = () => {
  const [data, setdata] = useState("");
  return <View></View>;
};

export default FabDebug;

const styles = StyleSheet.create({});
